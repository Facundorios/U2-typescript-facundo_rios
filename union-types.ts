// Declara una variable status que pueda ser de tipo string o número. Asígnale diferentes valores para probar ambos tipos.

let statuss: number | string = 0;
console.log(statuss);

statuss = "Activo";
console.log(statuss);
